# M23 P6 — Defect register

Every defect found while building and verifying the canonical Offer
workflow, whether in new P6 code, in the wiring, or in a frozen suite's
assumption that P6 legitimately supersedes. Severity follows §82.
Legacy impact says whether any pre-P6 data or behaviour was affected
(none was: no Offer existed before P6).

| ID | Severity | Component | Reproduction | Actual | Expected | Impact | Root cause | Fix | Regression | Status | Release-blocking? | Legacy impact |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| D-P6-1 | High | m23/evidence.mjs (wiring) | GET any room's journey after the Offer kinds were wired | 500 `INTERNAL` (`ReferenceError: now is not defined`) — every journey and every availability check | journey answers | every P5/P4B suite that reads a journey failed (m23DecisionE2E crashed, AT1) | the provider's `check(kind, { kase })` signature ignored the `now` the lifecycle already passed; the new Offer branch read it | `check(kind, { kase, now })` with a real-clock fallback | m23DecisionE2E, m23AgentIntegrationE2E AT1, m23OfferE2E E | fixed R1 | yes | none (never shipped) |
| D-P6-2 | High | m28/offer.mjs `offerIntegrity` | withdraw a DRAFT, then replay the withdraw key or read the Offer | 500 `OFFER_STATE_UNKNOWN` on the club's own Offer | 200 | a withdrawn draft made its Offer unreadable to the club for ever | integrity demanded `issuedAt`/`expiresAt` on every non-DRAFT revision; a withdrawn draft has neither | issued-like statuses require them; WITHDRAWN requires `withdrawnAt` | m23OfferE2E L4–L5, A70–A72 | fixed R1 | yes | none |
| D-P6-3 | High | m28/index.mjs recipient listing | withdraw a DRAFT; list as the recipient | a revision with status WITHDRAWN and no `issuedAt` counted as "issued" and could be listed | invisible | a draft nobody was sent could reach the recipient's list once withdrawn | `status !== 'DRAFT'` used as "was issued" | `wasIssued(r)` (= left DRAFT by being issued, `issuedAt` readable) everywhere: listing, receipts, documents, evidence, views | m23OfferE2E L6, P §1 | fixed R1 | yes | none |
| D-P6-4 | Medium | m28/offer.mjs `canRespondToRevision` | club opens revision 2 as a DRAFT while revision 1 is ISSUED; recipient accepts revision 1 | 409 `OFFER_SUPERSEDED` ("not the current one") | 200 — revision 1 is still what the recipient was sent | a club could freeze a recipient out of answering by opening a draft | the gate compared against the club's CURRENT (working) revision | the LIVE revision (latest issued) is the answerable one; an answer discards the unissued draft, recorded as `offer_draft_discarded` | m23OfferE2E K1, A54–A60, #42 | fixed R1 | yes | none |
| D-P6-5 | Medium | m28/index.mjs idempotency keys | issue revision 1 with key K1, issue revision 2 with K2, retry K1 | the retry was treated as a state error (or worse, could replay the wrong revision) because a single `keys.issue` slot was overwritten | 409 `OFFER_IDEMPOTENCY_CONFLICT` naming the different revision | a lost-response retry after a second issue could not be told apart from a state error | one slot per act | one list per act per Offer, never overwritten | m23OfferE2E Q3, P 3.9 | fixed R1 | yes (§27) | none |
| D-P6-6 | Medium | m28/index.mjs rev contract | edit/issue/withdraw sent the REVISION's rev; revise sent the OFFER's rev | clients had to know which counter each route wanted; a correct edit conflicted after a share | ONE rev, the Offer's, on every mutation | spurious `OFFER_REV_CONFLICT` for the club | two counters exposed by different routes; the recipient's share also bumped the Offer rev | every club mutation gates on `offer.rev`; a share/unshare and a read receipt do not bump it; recipient `expectedRev` (optional) checks the Offer rev | m23OfferE2E C1–C3, K3–K4, N4–N5 | fixed R1 | yes (§28) | none |
| D-P6-7 | Medium | m28/index.mjs recipient list (§40) | a recipient opens the app, which lists Offers with full terms, but never fetches one by id | the club's `firstViewedAt` stayed null although the recipient had seen the terms | a receipt on first sight | the club could not tell an unread Offer from a read one | receipts were recorded only on the by-id read | the list records one receipt per live revision (persisted once) | m23OfferLive C5 | fixed R2 | yes (§40) | none |
| D-P6-8 | Medium | scoutbox-agent nav.ts | open `#/clients/:rel/offers` | the hash fell back to Overview: the Offers tab was unreachable by deep link | the Offers tab | an agent could not open a client's Offers from a link or a reload | the client-hash regex carried a hardcoded tab list that did not include the new tab | `offers` added to the regex (the tab list and the regex are now asserted together by the live suite E1/E3) | m23OfferLive E1, E3 | fixed R2 | yes | none |
| D-P6-9 | Low | m182 event registry check | boot | warning "registry lists names the server never emits: offer_issued, offer_superseded"; m182E2E failed | silence | a false drift alarm | the issue route broadcast through a ternary, which the source scanner cannot see | two literal `broadcast('offer_superseded')` / `broadcast('offer_issued')` calls; the names declared in `EMITTED_EVENTS` | m182E2E, m23OfferE2E A15 | fixed R1 | no | none |
| D-P6-10 | Low | m23E2E Z2/Z3/Z5, m23BootContract, m23DecisionPersistence | run the frozen suites | three suites asserted P2's "recruitmentOffers is optional / unavailable / absent" | the shipped store is module-guaranteed and available | superseded truths, not product defects | P2 froze "not shipped yet" as a test | assertions updated to the P6 truth with comments (module-guaranteed by m28; migrations alone still create no Offer store) | the three suites | fixed R1 | no | none |
| D-P6-11 | Low | scoutbox-club/grassroots offerPanel | a foreign-club error mapped to a key that did not exist (`rm.notFound`) | m182E2E "no screen asks for a key that does not exist" failed | a real key in EN and FR | English fallback text in one refusal | a key typo | `of.err.ROOM_NOT_FOUND` added in EN/FR | m182E2E | fixed R2 | no | none |
| D-P6-14 | Medium | club/grassroots offerPanel, agent Offers tab, player M23Offer | switch the app to FR and open an Offer | the closing "honest" line (and the agent tab's intro/honest) rendered the SERVER's English sentence | app copy in the app's language | English text in a French screen (§45 parity; "no raw backend text") | the panels preferred `offer.honest` / `data.note` from the API over their own i18n keys | the apps render only their own EN/FR strings (`of.honest`, `offers.intro`/`offers.honest`, `offerNotSigning`); the API sentences remain for API consumers | m23OfferLive N16 (asserts no English honest line in FR) | fixed R2 | yes (§45) | none |
| D-P6-12 | Low (test) | m23OfferE2E | first runs | eleven test-side mistakes (legacy route code `ROOM_EVIDENCE_REQUIRED` vs `LIFECYCLE_EVIDENCE_REQUIRED`, `STATUS_EVIDENCE_REQUIRED` shape, `outcome.signing` path, table sizes, a traversal string routed elsewhere, the rate budget shared with earlier drafts, `expectedRev` sent without `revisionId`, a 404 body from an unregistered route in the shape sweep, comment text in the source sweep) | — | none in product | test authoring | corrected; every case now asserts status AND code | the suite itself (447 green) | fixed R1 | no | none |
| D-P6-13 | Low (test) | m23OfferLive | first runs | the player app keeps inactive tabs mounted but hidden (body `innerText` is empty for them), a section title renders uppercase, an over-strict "signed" regex, a minor's draft was forced to issue before its content was complete (content is validated before the recipient, §50) | — | none in product | test authoring | locator-scoped waits, case-insensitive title match, a precise no-signing-claim regex, the draft completed through the API first | the suite itself | fixed R2 | no | none |

## Totals

| Severity | Found | Fixed | Open |
| --- | --- | --- | --- |
| Critical | 0 | 0 | 0 |
| High | 3 | 3 | 0 |
| Medium | 6 | 6 | 0 |
| Low | 5 (2 product, 1 wiring, 2 test-side) | 5 | 0 |

Known P6 flakes: 0 (every suite listed in M23_P6_TEST_REPORT.md ran green
on its final run; the only re-runs were after code or test fixes, never
after an unexplained failure).
