# Backend the portal talks to (default http://localhost:4000).
VITE_API_URL=http://localhost:4000

# Explicit standalone demo (in-browser data, clearly labelled). Leave unset
# for the real, connected application.
VITE_DEMO=
